apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: django-app
  labels:
    release: monitoring   # MUST match Helm release name
spec:
  selector:
    matchLabels:
      app: django-app
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
